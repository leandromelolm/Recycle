<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">
    <fragment xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:map="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_weight="1"
        android:layout_width="match_parent"
        android:layout_height="0pt"
        map:uiZoomControls="true"
        map:cameraZoom="15"
        tools:context=".MapsActivity" />

    <Button
        android:id="@+id/discardButton"
        style="@style/Widget.AppCompat.Button.Colored"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_weight="0"
        android:background="@color/greenpeace"
        android:backgroundTint="@color/greenpeace"
        android:fontFamily="@font/roboto_bold"
        android:onClick="descarte"
        android:text="Confirme Descarte"
        android:textColor="#FFFFFF"
        android:textSize="15sp"
        app:backgroundTint="@null" />
    <!--Botão Solicitar coleta
    <Button
        android:id="@+id/requestdButton"
        style="@style/Widget.AppCompat.Button.Colored"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_weight="0"
        android:background="@color/white"
        android:backgroundTint="@color/white"
        android:enabled="false"
        android:fontFamily="@font/roboto_bold"
        android:text="Coletar"
        android:textColor="@color/greenpeace"
        android:textSize="15sp"
        app:backgroundTint="@null" />
        -->
</LinearLayout>